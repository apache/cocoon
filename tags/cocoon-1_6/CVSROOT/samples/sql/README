This directory shows an example of usage for the SQLProcessor.

Since this requires parameters that match your system, it cannot be run
out of the box but requires some modification. See the SQLProcessor docs
for how to do this.


                                           The Cocoon Development Team