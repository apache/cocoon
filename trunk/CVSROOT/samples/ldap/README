This directory shows an example of usage for the LDAPProcessor.

Since this requires parameters that match your system, it cannot be run
out of the box but requires some modification. See the LDAPProcessor docs
for how to do this.


                                           The Cocoon Development Team