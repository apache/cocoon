<?xml version="1.0"?>

<!-- ===================================================================

   Build file for Cocoon

Notes:
   This is a build file for use with the Jakarta Ant java build tool.

Installation Instructions:
   To install Ant on your system, you need to checkout the
   "jakarta-tools" CVS module. Then you should place the files
     - ant.jar
     - projectx-tr2.jar
     - javac.jar
   in your classpath.

Build Instructions:
   To build, run

     java org.apache.tools.ant.Main

   on the directory where this file is located.

Authors:
  Stefano Mazzocchi <stefano@apache.org>

Fixme:
  - design better package abstraction and build system
  - make better javadoc task

Copyright:
  Copyright (c) 1999 The Apache Software Foundation.

   $Id: build.xml,v 1.5 1999-12-08 12:29:05 stefano Exp $

==================================================================== -->

<project default="package" basedir=".">

  <property name="name" value="Cocoon"/>
  <property name="version" value="1.6-dev"/>
  <property name="year" value="1999"/>

  <property name="build.compiler" value="classic"/>
  <property name="debug" value="off"/>

  <property name="src.dir" value="./src"/>
  <property name="dtd.dir" value="./dtd"/>
  <property name="docs.dir" value="./docs"/>
  <property name="sheets.dir" value="./stylesheets"/>
  <property name="images.dir" value="./resources"/>
  <property name="samples.dir" value="./samples"/>
  <property name="version.file" value="org/apache/cocoon/Defaults.java"/>

  <property name="packages" value="org.apache.cocoon"/>

  <property name="build.dir" value="./build"/>
  <property name="build.src" value="./build/src"/>
  <property name="build.dest" value="./build/classes"/>
  <property name="build.docs" value="./build/docs"/>
  <property name="build.javadocs" value="./build/javadocs"/>

  <property name="sbk.docs" value="./build/sbkdocs"/>

  <property name="dist.dir" value="./dist"/>

  <property name="site.dir" value="../xml-site/sources/cocoon"/>
  <property name="site.docs" value="../xml-site/targets/cocoon"/>

  <property name="style" value="../xml-site/style"/>

  <!-- =================================================================== -->
  <!-- Prepares the build directory                                        -->
  <!-- =================================================================== -->
  <target name="prepare">
    <mkdir dir="${build.dir}"/>
  </target>

  <!-- =================================================================== -->
  <!-- Compiles the source code                                            -->
  <!-- =================================================================== -->
  <target name="compile" depends="prepare">
    <!-- create directories -->
    <mkdir dir="${build.src}"/>
    <mkdir dir="${build.dest}"/>

    <!-- copy src files -->
    <copydir src="${src.dir}" dest="${build.src}"/>

    <!-- substitute tokens as needed -->
    <replace file="${build.src}/${version.file}" token="@@version@@" value="${version}"/>
    <replace file="${build.src}/${version.file}" token="@@year@@" value="${year}"/>

    <!-- do the compilation -->
    <javac srcdir="${build.src}" destdir="${build.dest}" debug="${debug}"/>
  </target>

  <!-- =================================================================== -->
  <!-- Creates the class package                                           -->
  <!-- =================================================================== -->
  <target name="package" depends="compile">
    <!-- create the package -->
    <jar jarfile="${build.dir}/${name}.jar" basedir="${build.dest}" manifest="${build.src}/MANIFEST.mf" items="org"/>
  </target>

  <!-- =================================================================== -->
  <!-- Converts Docs to Stylebook DTDs                                     -->
  <!-- =================================================================== -->
  <target name="docs-convert" depends="prepare">
    <mkdir dir="${sbk.docs}"/>
    <xslt src="${docs.dir}/index.xml" dest="${sbk.docs}/index-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/installing.xml" dest="${sbk.docs}/installing-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/cocoon2.xml" dest="${sbk.docs}/cocoon2-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/dcpprocessor.xml" dest="${sbk.docs}/dcpprocessor-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/sqlprocessor.xml" dest="${sbk.docs}/sqlprocessor-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/license.xml" dest="${sbk.docs}/license-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/javadoc.xml" dest="${sbk.docs}/javadoc-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/guide.xml" dest="${sbk.docs}/guide-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/technologies.xml" dest="${sbk.docs}/technologies-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/dynamic.xml" dest="${sbk.docs}/dynamic-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/faq.xml" dest="${sbk.docs}/faq-sbk.xml" sheet="${sheets.dir}/faq-stylebook.xsl"/>
    <xslt src="${docs.dir}/WD-xsp.xml" dest="${sbk.docs}/WD-xsp-sbk.xml" sheet="${sheets.dir}/spec-stylebook.xsl"/>
    <xslt src="changes.xml" dest="${sbk.docs}/changes-sbk.xml" sheet="${sheets.dir}/changes-stylebook.xsl"/>
    <xslt src="todo.xml" dest="${sbk.docs}/todo-sbk.xml" sheet="${sheets.dir}/todo-stylebook.xsl"/>
    <copydir src="${images.dir}" dest="${sbk.docs}/images"/>
  </target>

  <!-- =================================================================== -->
  <!-- Generate HTML docs                                                  -->
  <!-- =================================================================== -->
  <target name="docs">
    <mkdir dir="${build.docs}"/>
    <copyfile src="${docs.dir}/docs-book.xml" dest="${sbk.docs}/book.xml"/>
    <stylebook book="${sbk.docs}/book.xml" dest="${build.docs}" style="${style}"/>
  </target>

  <!-- =================================================================== -->
  <!-- Creates the API documentation                                       -->
  <!-- =================================================================== -->
  <target name="javadocs" depends="prepare">
    <mkdir dir="${build.javadocs}"/>
    <copyfile src="${stylesheets}/javadoc.css" dest="{$build.javadocs}/stylesheet.css"/>
    <javadoc2 packagenames="${packages}"
              sourcepath="${build.src}"
              destdir="${build.javadocs}"
              author="true"
              version="true"
              use="true"
              windowtitle="${name} API"
              doctitle="${name}"
              bottom="Copyright &#169; ${YEAR} Apache XML Project. All Rights Reserved."
    />
  </target>

  <!-- =================================================================== -->
  <!-- Creates the distribution                                            -->
  <!-- =================================================================== -->
  <target name="dist" depends="package, docs, javadocs">
    <mkdir dir="${dist.dir}"/>
    <mkdir dir="${dist.dir}/bin"/>
    <mkdir dir="${dist.dir}/src"/>
    <mkdir dir="${dist.dir}/conf"/>
    <mkdir dir="${dist.dir}/docs"/>
    <mkdir dir="${dist.dir}/docs/api"/>
    <mkdir dir="${dist.dir}/samples"/>
    <mkdir dir="${dist.dir}/xdocs"/>
    <mkdir dir="${dist.dir}/xdocs/DTD"/>
    <mkdir dir="${dist.dir}/xdocs/stylesheets"/>

    <copydir src="${build.src}" dest="${dist.dir}/src" ignore="cocoon.properties"/>
    <copyfile src="${dist.dir}/src/org/apache/cocoon/cocoon.properties" dest="${dist.dir}/conf/cocoon.properties"/>
    <copyfile src="${build.dir}/${name}-${version}.jar" dest="${dist.dir}/bin/${name}-${version}.jar"/>
    <copydir src="${build.docs}" dest="${dist.dir}/docs"/>
    <copydir src="${build.javadocs}" dest="${dist.dir}/docs/api"/>
    <copydir src="${samples.src}" dest="${dist.dir}/samples"/>
    <copydir src="${docs.dir}" dest="${dist.dir}/xdocs" ignore="docs-book.xml, site-book.xml"/>
    <copydir src="${dtd.dir}" dest="${dist.dir}/xdocs/DTD"/>
    <copydir src="${sheets.dir}" dest="${dist.dir}/xdocs/stylesheets" ignore="javadoc.css"/>

    <copyfile src="README" dest="${dist.dir}/README"/>
    <replace file="${dist.dir}/README" token="@@version@@" value="${version}"/>
    <copyfile src="LICENSE" dest="${dist.dir}/LICENSE"/>
    <copyfile src="build.xml" dest="${dist.dir}/build.xml"/>

    <jar jarfile="${name}-${version}.jar" basedir="${dist.dir}" items="bin, src, conf, docs, samples, xdocs"/>
  </target>

  <!-- =================================================================== -->
  <!-- Cleans everything                                                   -->
  <!-- =================================================================== -->
  <target name="clean">
    <deltree dir="${build.dir}"/>
    <deltree dir="${dist.dir}"/>
  </target>

  <!-- =================================================================== -->
  <!-- Creates the web site                                                -->
  <!-- =================================================================== -->
  <target name="site">
    <copyfile src="${docs.dir}/site-book.xml" dest="${site.dir}/../cocoon.xml"/>
    <xslt src="${docs.dir}/index.xml" dest="${site.dir}/index-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/installing.xml" dest="${site.dir}/installing-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/cocoon2.xml" dest="${site.dir}/cocoon2-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/dcpprocessor.xml" dest="${site.dir}/dcpprocessor-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/sqlprocessor.xml" dest="${site.dir}/sqlprocessor-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/license.xml" dest="${site.dir}/license-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/javadoc.xml" dest="${site.dir}/javadoc-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/guide.xml" dest="${site.dir}/guide-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/technologies.xml" dest="${site.dir}/technologies-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/dynamic.xml" dest="${site.dir}/dynamic-sbk.xml" sheet="${sheets.dir}/document-stylebook.xsl"/>
    <xslt src="${docs.dir}/faq.xml" dest="${site.dir}/faq-sbk.xml" sheet="${sheets.dir}/faq-stylebook.xsl"/>
    <xslt src="${docs.dir}/WD-xsp.xml" dest="${site.dir}/WD-xsp-sbk.xml" sheet="${sheets.dir}/spec-stylebook.xsl"/>
    <xslt src="changes.xml" dest="${site.dir}/changes-sbk.xml" sheet="${sheets.dir}/changes-stylebook.xsl"/>
    <xslt src="todo.xml" dest="${site.dir}/todo-sbk.xml" sheet="${sheets.dir}/todo-stylebook.xsl"/>
    <copydir src="${images.dir}" dest="${site.dir}/images"/>
    <stylebook book="${site.dir}/../cocoon.xml" dest="${site.docs}" style="${style}"/>
  </target>

</project>
